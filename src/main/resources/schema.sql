CREATE TABLE Ticket
(
    movie       VARCHAR(255),
    nmbrTickets INTEGER,
    firstName   VARCHAR(255),
    surname     VARCHAR(255),
    email       VARCHAR(255),
    phonenmbr   INTEGER,
    id INTEGER AUTO_INCREMENT NOT NULL,
    PRIMARY KEY (id)
);