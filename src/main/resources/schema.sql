CREATE TABLE IF NOT EXISTS Ticket
(
    movie       VARCHAR(255),
    nmbrTickets INTEGER,
    firstName   VARCHAR(255),
    surname     VARCHAR(255),
    email       VARCHAR(255),
    phonenmbr   INTEGER,
);